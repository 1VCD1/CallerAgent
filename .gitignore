# Secrets — never commit real credentials
.env
.env.local
*.env

# Dependencies
node_modules/

# Large binaries (download separately)
cloudflared.exe
ngrok.exe

# One-off migration/query scripts (may contain hardcoded credentials)
backend/*.mjs

# Logs
*.log
server.log

# TypeScript build artifacts
dist/
*.tsbuildinfo

# Claude Code internal state
.claude/

# macOS
.DS_Store
